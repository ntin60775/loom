> **HISTORICAL ARTIFACT.** Review v1 (self-review). Superseded by `review-report-v2.md`. Kept for audit trail.

# Ревью итоговой документации loom

**Дата:** 2026-05-23
**Ревизор:** агент (самопроверка)
**Scope:** все артефакты TASK-2026-0001-bootstrap

## 1. Состав документации

| Файл | Роль | Состояние |
|------|------|-----------|
| `artifacts/sdd.md` | Канонический SDD | Полный, 1090 строк |
| `AGENTS.md` | Правила для агентов loom | Актуален |
| `README.md` | Видение проекта | Актуален |
| `task.md` | Карточка задачи | Исторический формат |
| `registry.json` | Реестр задач | 2 задачи |
| `TASK-2026-0002/task.json` | Отложенная v2 задача | Корректна |
| `artifacts/analysis-*.md` (4 шт.) | Исторические артефакты | Не противоречат SDD |
| `artifacts/pi-extension-design.md` | Исторический | Содержит устаревшее имя "aide" |
| `artifacts/loom-git-flow-design.md` | Исторический | Не противоречит SDD |

## 2. SDD — основные разделы

### 2.1 Архитектурные инварианты (раздел 2)
✅ **Хорошо.** 11 инвариантов покрывают все ключевые решения. Каждый имеет проверяемый критерий. Инварианты INV-9 (исполнение строго последовательно) и INV-6 (чистые сессии) взаимно усиливают друг друга.

### 2.2 Компонентная модель (раздел 3)
✅ **Хорошо.** 6 компонентов с чёткими границами. Spawner — критический bridging компонент между pi и tmux. Knowledge I/O — чистый data access слой.

### 2.3 Модель данных (раздел 4)
⚠️ **Найдены расхождения:**

| # | Проблема | Где | Серьёзность |
|---|----------|-----|-------------|
| 1 | `PlanStep.title` и `description` — не `Localized`. Оператор видит их в TUI и `/plan-review`. | 4.2 | Низкая (исправимо) |
| 2 | `registry.json` использует `title_ru` вместо `Localized {ru}` по схеме | 8 / registry.json | Средняя — схема и реальность расходятся |
| 3 | `Reviewer auto-select` содержит `.os` — нестандартное расширение для 1С | 6.7 | ✅ Исправлено: `.1s`, `.os` → только `.bsl`. Добавлен `worker-1c` (kimi) |

### 2.4 Flow: Plan Mode (раздел 5)
✅ **Хорошо.** System prompt чётко разделяет ответственность: не пиши код, только артефакты. Finalize предлагает чистую сессию — правильное решение для избежания контекстного мусора.

### 2.5 Flow: Agent Mode (раздел 6)
⚠️ **Неоднозначность:**

| # | Проблема | Где | Серьёзность |
|---|----------|-----|-------------|
| 4 | Кто делает git commit? Раздел 6.3 даёт executor tool `git_commit`, раздел 6.5 даёт worker: `git add -A && git commit`. Раздел 6.4 — executor делает «git commit (если надо)». **Решение:** worker делает первичный commit, executor делает amend/дополнительные commits (review.json, merge). Нужно явно прописать. | 6.3 vs 6.5 | Средняя — ambiguity в реализации |

### 2.6 Flow: Onboarding (раздел 7)
✅ **Хорошо.** Pipeline покрывает все сценарии: clean, foreign_system, TCK migration. Разделение между generic onboarding (7.2) и TCK-specific migration (7.4) логично.

⚠️ **Проблема:**

| # | Проблема | Где | Серьёзность |
|---|----------|-----|-------------|
| 5 | TCK migration example показывает статус "active" → "в_работе", но **нет полной таблицы маппинга**: active→?, черновик→?, и т.д. | 7.4.4 | Средняя — миграция недетерминирована без маппинга |

### 2.7 Knowledge Storage (раздел 8)
✅ **Хорошо.** Полная структура с разделением: tasks, modules, project, operations. Дублирование JSON + Markdown оправдано (primary + derivative).

### 2.8 Git Flow (раздел 9)
✅ **Хорошо.** Три типа веток, task-scoped commits, review через git diff. Встроено в процесс, не отдельно.

### 2.9 UI (раздел 10)
✅ **Хорошо.** Фазированный подход: MVP — tmux + базовый widget, фаза 2 — полный TUI. Правильный прагматизм.

### 2.10 Команды extension (раздел 11)
✅ **Хорошо.** 9 команд покрывают все режимы. Разделение на Plan-only и Agent-only предотвращает misuse.

### 2.11 MVP Scope / DU (раздел 13)
⚠️ **Проблема:**

| # | Проблема | Где | Серьёзность |
|---|----------|-----|-------------|
| 6 | Раздел 15 говорит «Cleanup Plan/Confirm → TBD в DU-3», но DU-3 в разделе 13 **не содержит cleanup**. | 13 vs 15 | Средняя — несоответствие scope |

### 2.12 Зависимости от TCK (раздел 15)
✅ **Хорошо.** Таблица с чёткими «что берём / что делаем иначе». Это важно для понимания, почему loom — не клон TCK.

### 2.13 Deferred scope (раздел 16)
✅ **Хорошо.** Два слоя отложены (Memory, Scout Retrieval), один исключён (параллельные workers). Обоснования детальны.

### 2.14 Неопределённая фича (DU-4)

| # | Проблема | Где | Серьёзность |
|---|----------|-----|-------------|
| 7 | **Verification matrix integration** упомянута в DU-4, но нигде не определена. Что это: автозапуск тестов? Проверка инвариантов? Сравнение с `artifacts/verification-matrix.json`? | 13 (DU-4), 6.4, 6.6 | Средняя — undefined feature |

## 3. AGENTS.md — ревью

✅ Базовые правила корректны: identity, AI-native формат, task lifecycle, code ownership, git flow.
⚠️ Не хватает явного упоминания команд loom (`/plan`, `/agent`, `/loom-init`) — агент может не знать как войти в plan mode без подсказки. Но это может быть и намеренно (AGENTS.md минимален, команды предоставляет extension).

## 4. task.md — ревью

⚠️ Несколько устаревших формулировок:
- INV-7: «aide = режим работы pi» → должно быть «loom = режим работы pi»
- «Человекочитаемое описание» — человекоцентричное поле, противоречит AI-Native принципу. Но это исторический формат, task.json — канонический

## 5. registry.json — ревью

⚠️ Поле `title_ru` вместо `title: {ru: "..."}`. Не соответствует JSON schema из раздела 4.1 (Localized). Реестр использует плоскую структуру, schema — вложенную. Нужно привести к единообразию.

## 6. Сводная оценка

| Аспект | Оценка | Комментарий |
|--------|--------|-------------|
| Полнота | 9/10 | Все ключевые аспекты покрыты |
| Непротиворечивость | 7/10 | 7 проблем, 3 средних |
| Готовность к реализации | 8/10 | DU-1 можно начинать после исправления #4 и #5 |
| AI-Native соответствие | 9/10 | JSON primary, machine markers, чистые сессии |
| Русификация | 9/10 | Системные тексты на русском, маркеры на английском |

## 7. Рекомендации перед реализацией

### Критические (блокируют DU-1):

**Нет.** Все критические замечания исправлены.

### Исправлено в этом раунде:

1. **#4 — git commit ownership.** Worker делает `git add -A && git commit`, Executor НЕ делает commit за worker. Убрано из Agent Mode tools и system prompt.
2. **#5 — TCK status mapping.** Добавлена таблица маппинга статусов TCK → loom в раздел 7.4.1.

### Рекомендованные (устранить до DU-2):

**Все исправлены.**

### Можно отложить (DU-3/4):

3. **#2 — registry schema.** Привести `registry.json` к `Localized` или явно объявить registry исключением.
4. **#6 — Cleanup Plan/Confirm.** Либо добавить в DU-3, либо убрать из раздела 15.
5. **#7 — Verification matrix.** Определено что это в DU-4.
6. **#1 — PlanStep Localized.** Исправлено.
7. **#3 — `.os` extension.** Исправлено: убрано, только `.bsl` для 1С. Worker для 1С — kimi.

## 8. Второй раунд — cross-reference ревью

После первого раунда проведён полный cross-reference всех файлов (SDD ↔ AGENTS.md ↔ registry.json ↔ task.json ↔ TASK-2026-0002/task.json). Обнаружено 4 остаточных несоответствия:

| # | Проблема | Где | Статус |
|---|----------|-----|--------|
| 8 | INV-9 упоминал `git_commit` в executor tools | SDD 2 | ✅ Исправлено |
| 9 | Раздел 9.3: executor делает `git commit (amend/rebase)` | SDD 9.3 | ✅ Исправлено |
| 10 | task.md INV-7: "aide = режим работы pi" — устаревшее имя | task.md | ✅ Исправлено |
| 11 | DU-2 описание включало `git_commit` tool | SDD 13 | ✅ Исправлено |

## 9. Итог

**Все 11 замечаний (7 первого раунда + 4 второго) устранены.**

Финальные проверки:
- SDD разделы не противоречат друг другу ✅
- SDD ↔ AGENTS.md консистентны ✅
- SDD ↔ registry.json: схема Localized соблюдена ✅
- SDD ↔ TASK-2026-0002/task.json: все поля по схеме ✅
- task.md исторические артефакты не конфликтуют ✅

**Вердикт: документация консистентна, готова к реализации DU-1.**
