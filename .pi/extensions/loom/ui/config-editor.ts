/**
 * Config Editor — TUI for editing execution-config and subagent-config
 *
 * Actual logic lives in index.ts (/loom-config command) and agent-mode/tools.ts (loom_edit_config).
 * This module serves as the UI entry point for future form-based editing.
 */

export {};
